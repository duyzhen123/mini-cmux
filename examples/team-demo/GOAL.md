# Demo goal

Create `demo_output.txt` containing exactly:

```text
mini-cmux team handoff complete
```

The planner must write `PLAN.md`, the implementer must create the output, and
the verifier must confirm the exact content.
